fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.8.2
python-multipart==0.0.9
Pillow==10.4.0
pyjwt==2.9.0
httpx==0.27.0
prometheus-client==0.20.0

# PyTorch stack: pin to verified versions (CPU by default). For CUDA, override via index-url.
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2

diffusers==0.30.0
transformers==4.44.2
accelerate==0.34.2
safetensors==0.4.4

# Optional dev tools (install manually if needed):
pytest==8.2.0
# ruff==0.6.2

# For CUDA 12.x installation example:
# pip install torch==2.2.2 torchvision==0.17.2 torchaudio==2.2.2 --index-url https://download.pytorch.org/whl/cu121